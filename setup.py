from distutils.core import setup
# from Cython.Build.Dependencies import cythonize

setup(
    # ext_modules=cythonize(
    #     "cs336_basics/tokenizer_merge.pyx", compiler_directives={"language_level": "3"}
    # )
)
